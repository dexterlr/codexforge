# Scratch Docs

This directory is for temporary notes only.

Current product documentation lives in:

- `../README.md` for the root product overview.
- `README.md` in the frontend directory for local development commands.
- `docs/architecture/` for domain architecture and data flow.
- `docs/product/` for mission and product principles.
- `docs/operator/` for approval-gated operator scope.

Do not use scratch notes as source-of-truth product documentation.
