'use client';

import { MultiProviderCapabilityRoutingRoutePanel } from '../../multi-provider-capability-routing-map/components';

export function MultiProviderCapabilityResponseWiringPanel() {
  return <MultiProviderCapabilityRoutingRoutePanel routeSlug='multi-provider-capability-response-wiring' />;
}
