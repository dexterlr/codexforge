import type { CSSProperties } from "react"; import type { ModelCapabilityEntry } from "../model-capability-types";
export function ModelCapabilityEntryPanel({ entry }: { entry: ModelCapabilityEntry }) { return <article style={card}><h2 style={title}>{entry.family}</h2><p style={copy}>{entry.description}</p><p style={copy}>Provider: {entry.provider}. {entry.manualOnly ? "Manual browser only." : "API automation capable when configured."}</p></article>; }
const card: CSSProperties = { background:"rgba(2,6,23,0.68)", border:"1px solid rgba(148,163,184,0.16)", borderRadius:8, display:"grid", gap:6, padding:14 }; const title: CSSProperties = { fontSize:18, letterSpacing:0, margin:0 }; const copy: CSSProperties = { color:"#cbd5e1", fontSize:13, lineHeight:1.45, margin:0 };
