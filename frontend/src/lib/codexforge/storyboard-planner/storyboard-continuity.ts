import type { StoryboardContinuity } from "./storyboard-planner-types";

export function buildStoryboardContinuity(): StoryboardContinuity {
  return { id: "storyboard-continuity", continuityNotes: ["same subject across shots", "same style and lighting", "simple motion direction", "clear start and finish"], plainEnglish: "Continuity means the shots look like they belong to one video." };
}
