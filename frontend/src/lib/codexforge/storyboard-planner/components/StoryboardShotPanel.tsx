"use client";
import type { CSSProperties } from "react";
import type { StoryboardShot } from "../storyboard-planner-types";
export function StoryboardShotPanel({ shots }: { shots: StoryboardShot[] }) { return <article style={card}><h2 style={title}>Shots</h2>{shots.map((shot) => <div key={shot.id} style={row}><strong>{shot.order}. {shot.title}</strong><p style={copy}>{shot.visualDescription}</p><p style={copy}>Camera movement: {shot.cameraMovement}. Subject movement: {shot.subjectMovement}. Duration target: {shot.durationTarget}.</p><p style={copy}>Keyframe need: {shot.keyframeNeed}. Local draft suitability: {shot.localDraftSuitability}. Risk notes: {shot.riskNotes}. Next step: {shot.nextStep}.</p></div>)}</article>; }
const card: CSSProperties = { background: "rgba(2,6,23,0.68)", border: "1px solid rgba(148,163,184,0.16)", borderRadius: 8, display: "grid", gap: 10, padding: 14 }; const row: CSSProperties = { borderTop: "1px solid rgba(148,163,184,0.12)", display: "grid", gap: 5, paddingTop: 8 }; const title: CSSProperties = { fontSize: 18, letterSpacing: 0, margin: 0 }; const copy: CSSProperties = { color: "#cbd5e1", fontSize: 13, lineHeight: 1.5, margin: 0 };
