'use client';

import { MultiProviderCapabilityRoutingRoutePanel } from '../../multi-provider-capability-routing-map/components';

export function MultiProviderRetryRoutingWiringPanel() {
  return <MultiProviderCapabilityRoutingRoutePanel routeSlug='multi-provider-retry-routing-wiring' />;
}
