'use client';

import { MultiProviderCapabilityRoutingRoutePanel } from '../../multi-provider-capability-routing-map/components';

export function MultiProviderEditingRoutingWiringPanel() {
  return <MultiProviderCapabilityRoutingRoutePanel routeSlug='multi-provider-editing-routing-wiring' />;
}
