"use client";

import { PreviewFoundationEmptyState } from "../../video-foundation-ui";

export function CreativeCostSaverEmptyState() {
  return (
    <PreviewFoundationEmptyState
      title="No creative job selected"
      message="Choose a cost-saving route later. A beginner can start with local planning, then review drafts before deciding whether cloud is worth it."
    />
  );
}
