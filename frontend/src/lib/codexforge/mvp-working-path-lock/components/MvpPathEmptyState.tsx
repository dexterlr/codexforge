"use client";
import { copy, eyebrow, panel, title } from "./MvpPathStyles";
export function MvpPathEmptyState() { return <section style={panel} data-codexforge-mvp-path-empty="MvpPathEmptyState renders no duplicate route chip cloud advanced routes secondary"><span style={eyebrow}>Empty state</span><h2 style={title}>No run selected</h2><p style={copy}>Open the locked MVP path, then move one route at a time.</p></section>; }
