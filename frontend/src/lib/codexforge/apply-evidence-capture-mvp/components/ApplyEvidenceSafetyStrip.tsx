"use client";
import type { CSSProperties } from "react";
export function ApplyEvidenceSafetyStrip() { return <div style={strip} data-codexforge-apply-evidence-safety="ApplyEvidenceSafetyStrip renders no auto-apply no auto-run no auto-persistence no Brain mutation no memory auto-promotion preserve latest-message authority"><span>no auto-apply</span><span>no auto-run</span><span>copyable/exportable only</span><span>preserve latest-message authority</span></div>; }
const strip: CSSProperties = { display: "flex", flexWrap: "wrap", gap: 8, color: "#bae6fd", fontSize: 12, fontWeight: 800 };
