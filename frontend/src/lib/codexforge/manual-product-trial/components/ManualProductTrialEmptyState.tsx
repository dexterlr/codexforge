import { card, muted } from "./ComponentStyles";
export function ManualProductTrialEmptyState() { return <section style={card}><strong>Ready for operator review</strong><p style={muted}>Use the copy actions and record real evidence. This page does not mutate files, Brain graph, memory, or runtime events.</p></section>; }
