"use client";
import { copy, eyebrow, panel, title } from "./FrictionFixStyles";
export function FrictionFixEmptyState() { return <section style={panel} data-codexforge-friction-fix-empty="FrictionFixEmptyState renders empty state unhelpful fix prompt plain English"><span style={eyebrow}>Empty state</span><h2 style={title}>No friction selected</h2><p style={copy}>Use the default findings from the manual trial, then copy the smallest safe fix plan.</p></section>; }
