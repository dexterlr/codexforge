import type { CSSProperties } from "react"; import type { TaskModelProviderCandidate } from "../task-model-router-types";
export function TaskModelProviderCandidatePanel({ candidate }: { candidate: TaskModelProviderCandidate }) { return <article style={card}><h2 style={title}>{candidate.family}</h2><p style={copy}>{candidate.reason}</p><p style={copy}>{candidate.blocked ? "Blocked until configured." : candidate.manualOnly ? "Manual browser route." : "Recommendation only."}</p></article>; }
const card: CSSProperties={background:"rgba(2,6,23,0.68)",border:"1px solid rgba(148,163,184,0.16)",borderRadius:8,display:"grid",gap:6,padding:14}; const title: CSSProperties={fontSize:18,letterSpacing:0,margin:0}; const copy: CSSProperties={color:"#cbd5e1",fontSize:13,lineHeight:1.45,margin:0};
