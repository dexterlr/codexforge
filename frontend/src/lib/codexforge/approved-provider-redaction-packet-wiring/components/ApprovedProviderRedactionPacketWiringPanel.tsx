'use client';

import { FirstApprovedProviderExecutionBridgeRoutePanel } from '../../first-approved-provider-execution-bridge-map/components';

export function ApprovedProviderRedactionPacketWiringPanel() {
  return <FirstApprovedProviderExecutionBridgeRoutePanel routeSlug='approved-provider-redaction-packet-wiring' />;
}
