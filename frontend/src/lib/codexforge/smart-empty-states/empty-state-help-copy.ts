import type { EmptyStateHelpCopy } from "./smart-empty-state-types";

export function buildEmptyStateHelpCopy(): EmptyStateHelpCopy {
  return { title: "Helpful empty-state copy", copy: "Tell the user what is missing, why it matters, the next safe action, and the route to use." };
}
