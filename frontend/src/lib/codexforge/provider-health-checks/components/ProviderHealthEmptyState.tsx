import type { CSSProperties } from "react";
export function ProviderHealthEmptyState() { return <p style={copy}>If nothing is configured yet, start with manual ChatGPT or local/private prepass, then add one API key later.</p>; }
const copy: CSSProperties = { color: "#cbd5e1", fontSize: 13, lineHeight: 1.45, margin: 0 };
