import type { DraftRenderHandoff, DraftRenderRequest } from "./local-draft-render-types";
export function buildDraftRenderHandoff(request: DraftRenderRequest): DraftRenderHandoff { return { draftRequest: `Draft request: prompt=${request.promptExists}, storyboard=${request.storyboardExists}, keyframes=${request.keyframePlanExists}, workflow=${request.workflowSelected}, duration=${request.durationTarget}, resolution=${request.resolutionTarget}.`, renderHandoff: "Future render handoff: require approval, local provider, short draft settings, artifact capture, recovery path, no cloud spend, no auto-run.", nextStep: "Manual review remains required before any local render." }; }
