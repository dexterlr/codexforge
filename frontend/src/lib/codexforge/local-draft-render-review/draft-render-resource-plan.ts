import type { DraftRenderResourcePlan } from "./local-draft-render-types";
export function buildDraftRenderResourcePlan(): DraftRenderResourcePlan { return { id: "draft-render-resource-plan", gpuTimePosture: "short local GPU test only after approval", dualGpuNote: "If dual GPU is available, keep one draft job at a time until the queue is reviewed.", draftVsFinal: "Draft is short and low resolution; final remains a later decision.", localFirstSavings: "Local-first review can save cloud credits by catching bad prompts before paid generation.", queueReadiness: "Queue is planned, not running." }; }
