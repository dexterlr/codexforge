import type { DraftRenderRequest } from "./local-draft-render-types";
export function buildDraftRenderRequest(input: Partial<DraftRenderRequest> = {}): DraftRenderRequest { return { id: input.id ?? "draft-render-request-local", promptExists: input.promptExists ?? true, storyboardExists: input.storyboardExists ?? true, keyframePlanExists: input.keyframePlanExists ?? true, workflowSelected: input.workflowSelected ?? true, durationTarget: input.durationTarget ?? "4 to 6 seconds", resolutionTarget: input.resolutionTarget ?? "draft-sized 512p or lower", localProviderPlanned: input.localProviderPlanned ?? true, artifactPlanExists: input.artifactPlanExists ?? true, recoveryPathExists: input.recoveryPathExists ?? true }; }
export function buildDefaultDraftRenderRequest(): DraftRenderRequest { return buildDraftRenderRequest(); }
