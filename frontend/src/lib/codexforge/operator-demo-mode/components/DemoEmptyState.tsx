"use client";
import { copy, eyebrow, panel, title } from "./DemoStyles";
export function DemoEmptyState() { return <section style={panel} data-codexforge-demo-empty="DemoEmptyState renders advanced details hidden no giant raw JSON above fold"><span style={eyebrow}>Empty state</span><h2 style={title}>No demo evidence supplied</h2><p style={copy}>The demo can show the path, but success claims require real evidence and validation output.</p></section>; }
