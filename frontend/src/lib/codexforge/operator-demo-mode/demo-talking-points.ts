import type { DemoTalkingPoints } from "./operator-demo-types";

export function buildDemoTalkingPoints(): DemoTalkingPoints {
  return { proves: ["one safe coding path is understandable", "guarded apply review stays approval-first", "evidence and validation are captured instead of fabricated", "run history and release audit are reachable"], remainsManual: ["operator selects the file", "operator approves apply boundary", "operator runs validation outside the UI", "operator pastes output and decides pass/fail"] };
}
