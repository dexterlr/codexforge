'use client';

import { MultiProviderCapabilityRoutingRoutePanel } from '../../multi-provider-capability-routing-map/components';

export function MultiProviderCapabilityRequestWiringPanel() {
  return <MultiProviderCapabilityRoutingRoutePanel routeSlug='multi-provider-capability-request-wiring' />;
}
