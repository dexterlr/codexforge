"use client";
import type { CSSProperties } from "react";
import type { KeyframeEntry } from "../keyframe-plan-types";
export function KeyframeEntryPanel({ entries }: { entries: KeyframeEntry[] }) { return <article style={card}><h2 style={title}>Keyframes</h2>{entries.map((entry) => <div key={entry.id} style={row}><strong>{entry.order}. {entry.framePurpose}</strong><p style={copy}>Shot id: {entry.shotId}. Visual prompt: {entry.visualPrompt}</p><p style={copy}>Style reference note: {entry.styleReferenceNote}. Subject consistency note: {entry.subjectConsistencyNote}. Camera note: {entry.cameraNote}.</p><p style={copy}>Readiness status: {entry.readinessStatus}. Local generation suitability: {entry.localGenerationSuitability}. Next action: {entry.nextAction}.</p></div>)}</article>; }
const card: CSSProperties = { background: "rgba(2,6,23,0.68)", border: "1px solid rgba(148,163,184,0.16)", borderRadius: 8, display: "grid", gap: 10, padding: 14 }; const row: CSSProperties = { borderTop: "1px solid rgba(148,163,184,0.12)", display: "grid", gap: 5, paddingTop: 8 }; const title: CSSProperties = { fontSize: 18, letterSpacing: 0, margin: 0 }; const copy: CSSProperties = { color: "#cbd5e1", fontSize: 13, lineHeight: 1.5, margin: 0 };
