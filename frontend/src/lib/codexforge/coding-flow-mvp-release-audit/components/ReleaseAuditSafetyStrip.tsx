"use client";
import type { CSSProperties } from "react";
export function ReleaseAuditSafetyStrip() { return <div style={strip} data-codexforge-release-audit-safety="ReleaseAuditSafetyStrip renders no auto-apply no auto-run no direct unsafe UI tool calls preserve latest-message authority"><span>no auto-apply</span><span>no auto-run</span><span>approval required</span><span>preserve latest-message authority</span></div>; }
const strip: CSSProperties = { display: "flex", flexWrap: "wrap", gap: 8, color: "#bae6fd", fontSize: 12, fontWeight: 800 };
