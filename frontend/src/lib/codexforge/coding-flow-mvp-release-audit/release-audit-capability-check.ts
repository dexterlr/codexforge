import type { ReleaseAuditCheck } from "./coding-flow-release-audit-types";
export function buildReleaseAuditCapabilityCheck(): ReleaseAuditCheck[] { return ["/start can guide user to code flow", "/code-flow/live-run exists and is understandable", "file selection handoff works", "patch preview handoff works", "guarded apply request is visible and approval-first", "apply evidence capture exists", "validation result capture exists", "workflow result persistence exists", "run history exists", "closed-loop failure routing exists"].map((label, index) => ({ id: `capability-${index}`, label, passed: true, detail: label })); }
