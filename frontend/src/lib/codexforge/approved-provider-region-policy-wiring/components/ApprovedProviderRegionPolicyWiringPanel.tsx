'use client';

import { FirstApprovedProviderExecutionBridgeRoutePanel } from '../../first-approved-provider-execution-bridge-map/components';

export function ApprovedProviderRegionPolicyWiringPanel() {
  return <FirstApprovedProviderExecutionBridgeRoutePanel routeSlug='approved-provider-region-policy-wiring' />;
}
