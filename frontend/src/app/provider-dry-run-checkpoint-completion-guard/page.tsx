import { ProviderDryRunCheckpointCompletionGuardPageClient } from "./page-client";

export default function ProviderDryRunCheckpointCompletionGuardPage() {
  return <ProviderDryRunCheckpointCompletionGuardPageClient />;
}
