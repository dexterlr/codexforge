'use client';

import { FirstApprovedProviderExecutionBridgePageClientShell } from '@/lib/codexforge/first-approved-provider-execution-bridge-map/components';

export function ApprovedProviderRegionPolicyWiringPageClient() {
  return <FirstApprovedProviderExecutionBridgePageClientShell routeSlug='approved-provider-region-policy-wiring' />;
}
